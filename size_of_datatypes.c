#include<stdio.h>

int main(){


  int a;
  float pi;
  double num;

  printf("%zu\n",sizeof(a));
  printf("%zu\n",sizeof(pi));
  printf("%zu",sizeof(num));
  //%zu is used instead of format specifier for exact value


  return 0;
  
}